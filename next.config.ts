
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com",'res.cloudinary.com'],
  },
};
// Exemplo que pode causar confusão

module.exports = nextConfig;
